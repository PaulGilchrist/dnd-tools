import MonsterStats from '../common/MonsterStats';

/**
 * 5e MonsterStats component - Re-exports common MonsterStats
 * The adapter normalizes the data before passing to common component
 */
export default MonsterStats;
